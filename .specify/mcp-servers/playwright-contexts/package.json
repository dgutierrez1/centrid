{
  "name": "playwright-contexts-mcp",
  "version": "1.0.0",
  "description": "Custom Playwright MCP server with isolated browser context support for parallel sub-agent execution",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "playwright-contexts-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "playwright": "^1.48.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
