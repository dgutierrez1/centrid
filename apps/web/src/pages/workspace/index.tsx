import { WorkspaceLayout } from '@/components/layout/WorkspaceLayout';

/**
 * Workspace Page
 * Main workspace route with three-panel layout for document editing
 */
export default function WorkspacePage() {
  return <WorkspaceLayout />;
}
