# Supabase Database Connection (for Drizzle ORM)
# Get from: Supabase Dashboard → Settings → Database → Connection String
# 
# IMPORTANT: URL-encode special characters in password:
#   ! = %21    @ = %40    # = %23    $ = %24    % = %25
#   ^ = %5E    & = %26    * = %2A    ( = %28    ) = %29
#
# SESSION MODE (Port 5432) - Use for migrations and development
# Best for: drizzle-kit push, schema changes, local development
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# TRANSACTION MODE (Port 6543) - Use for Edge Functions runtime
# Best for: Serverless functions, production runtime with connection pooling
# Uncomment and set in Supabase Dashboard → Edge Functions → Secrets for production
# DATABASE_URL_POOLER="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-us-east-1.pooler.supabase.com:6543/postgres"

# Recommendations:
# - Development: Use port 5432 (SESSION MODE)
# - Production Edge Functions: Use port 6543 (TRANSACTION MODE) via Supabase Secrets
# - Migrations: Always use port 5432 (SESSION MODE)
