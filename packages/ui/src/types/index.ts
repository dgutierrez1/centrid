/**
 * Type exports from packages/ui
 * Central export point for all shared types
 */

// Agent system types (ContentBlock, etc.)
export * from './agent';

// File system types (FileSystemNode)
export * from './filesystem';
