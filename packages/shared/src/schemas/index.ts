/**
 * Centralized export for all validation schemas
 * Used by both frontend and backend for consistent validation
 */

// Auth schemas (signup, login, password reset)
export * from './auth.schema'

// Profile schemas (update, deletion)
export * from './profile.schema'
