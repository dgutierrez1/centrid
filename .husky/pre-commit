#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run type checking
echo "  • Type checking..."
npm run type-check
if [ $? -ne 0 ]; then
  echo "  ✗ Type check failed"
  exit 1
fi

# Run linting (API only for now)
echo "  • Linting API code..."
npm run lint -- apps/api/src/functions --max-warnings=0 2>/dev/null || true

echo "✓ Pre-commit checks passed"
